# Paths are relative to the repo root. Raw data is not committed to git
# (see .gitignore + README "Data" section for how to obtain it);
# artifacts/ (feature store, trained models, submissions) is regenerated by
# scripts/build_features.py, ecup_ltv.train and ecup_ltv.predict.
raw_path: data/raw/train.parquet
sample_submission_path: data/raw/sample_submit.csv

features_store_dir: artifacts/features_store
models_dir: artifacts/models
submissions_dir: artifacts/submissions
